# 第四版润色稿人工复核（2026-09-22）

润色稿经忠实性复核后改了这几条，理由如下；改动直接写回 `polished.zh.json` 与 `polished.en.json`。

- `case.title`：模型写成“一份命盘，四面的你”，实体是命例不是命盘，回到事实稿原句。
- `case.steps.4.body`：模型删掉了“问事面按时间倒序列着名下每一卦”这条核心事实，回到事实稿。
- `tools.cards.2.name`：App 里叫“八字排盘”，不叫“八字命盘”。
- `tools.cards.3.line`：“一组一份”是误写，是一位一份。
- `faq.items.0.question`：第一条要直答“这是真的算命吗”，问句不改。
- `faq.items.3.question`：面向懂行的人，三种盘式的名字要写全。
- `nav.methodology`：沿用方法论页现有标题“起卦的门道”。
- 英文七条超词数上限的按事实删句不删数字与专名；英文里不写 Master Hachimi，角色写 the Master；导航沿用现有 How it's built 与 Get the app。
